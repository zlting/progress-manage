<view class="main">
  <view class="text">{{obj.bannerContent}}</view>
  <image src="{{item}}" data-src="{{item}}" bindtap="previewImage" mode="widthFix" wx:for="{{obj.bannerImgs}}" wx:key="{{index}}"></image>
</view>